pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'echo "build example"'
            }
        }
        stage('test') {
            steps {
                sh 'echo "test example"'
            }
        }
        stage('deploy') {
            steps {
                sh 'echo "deploy example"'
            }
        }
    }
}

